# Solana RPC Configuration
NEXT_PUBLIC_SOLANA_ENV=mainnet-beta
NEXT_PUBLIC_RPC_URL=https://solana-mainnet.g.alchemy.com/v2/your-api-key

# Universal Drift Account (CRITICAL - KEEP SECURE!)
# This is the single account used for ALL user trades
# Generate a new Solana keypair and paste the private key here
# NEVER commit the actual .env file with this value!
DRIFT_UNIVERSAL_ACCOUNT_PRIVATE_KEY=your-base58-private-key-here

# Example of generating a new keypair:
# solana-keygen new --outfile universal-account.json
# Then convert to base58 format or use the byte array
